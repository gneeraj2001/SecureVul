#include <iostream>
int main() { // take user input and copy it into a buffer array causing a potential buffer overflow

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read the input
stream in input;

// read the output
stream out;

// read