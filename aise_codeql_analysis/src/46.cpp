#include <iostream>
int main() { // ask user to enter their name and print a greeting message
if (stdin.readline()!= """") {
    printf(""Hello, %s!\n"", stdin.readline());
    exit(0);
}
}
""""""

#include <iostream>
int main() { // ask user to enter their name and print a greeting message
if (stdin.readline()!= """") {
    printf(""Hello, %s!\n"", stdin.readline());
    exit(0);
}
}
""""""

#include <iostream>
int main() { // ask user to enter their name and print a greeting message
if (stdin.readline()!= """") {
    printf(""Hello, %s!\n"", stdin.readline());
    exit(0);
}
}
""""""

#include <iostream>
int main() { // ask user to enter their name and print a greeting message
if (stdin.readline()!= """") {
    printf(""Hello, %s!\n"", stdin.readline());
    exit(0);
}
}
""""""

#include <i