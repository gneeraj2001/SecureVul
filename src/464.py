import sys
def readAndPrintByteArray():
    # read input from standard input and store it in a byte array
    # print the byte array
    byte_array = bytearray(400)
    sys.stdin.readinto(byte_array)
    print(byte_array)

def readAndPrintByteArray2():
    # read input from standard input and store it in a byte array
    # print the byte array
    byte_array = bytearray(400)
    sys.stdin.readinto(byte_array)
    print(bytearray(byte_array))

def readAndPrintByteArray3():
    # read input from standard input and store it in a byte array
    # print the byte array
    byte_array = bytearray(400)
    sys.stdin.readinto(byte_array)
    print(bytearray(bytearray(byte_array)))

def readAndPrintByteArray4():
    # read input from standard input and store it in a byte array
    # print the byte array
    byte_array = bytearray(400)
    sys.stdin.readinto(byte_array)
    print(bytearray(bytearray(bytearray(byte_array))))

def readAndPrintByteArray5():
    # read input from standard input and store it in a byte array
    # print the byte array
    byte_array = bytearray(400)