#include <iostream>
int main() { // declare a character array named buffer of size 10 and read input into it using cin
    char buffer[10];
    cin >> buffer;
    cout << buffer << endl;
    return 0;
}